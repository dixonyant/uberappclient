import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { ApprovePickupPage } from './approve-pickup';

@NgModule({
  declarations: [
    ApprovePickupPage,
  ],
  imports: [
    IonicPageModule.forChild(ApprovePickupPage),
  ],
})
export class ApprovePickupPageModule {}
